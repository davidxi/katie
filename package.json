{
  "name": "katie-gh-pages",
  "version": "0.0.1",
  "private": true,
  "devDependencies": {
    "http-server": "^0.6.1",
    "tmp": "0.0.23",
    "bower": "^1.3.1"
  },
  "scripts": {
    "prestart": "npm install",
    "start": "http-server -p 3000"
  }
}
